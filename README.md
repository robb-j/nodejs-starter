# Rob's Node.js starter

- Node configured with `.node-version` + ESM
- Prettier development dependency + configured
- `.editorconfig`
- TypeScript with `tsconfig.json`

**TODOs**

- Update package name
- Remove these todos
